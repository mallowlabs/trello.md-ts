// type t = any;

// let create = (name: string): t => new (require("trello-browser"))(name);

// let set_token = (obj: t, token: string): void => obj.setToken(token);
// let token = (obj: t): string => obj.token;

// let get = (obj: t, url: string): Promise<any> => obj.get(url);

// let auth = (
//   obj: t,
//   options: {
//     name: string;
//     expiration: string;
//     scope: { read: boolean; write: boolean; account: boolean };
//   }
// ): Promise<void> => obj.auth(options);
